/**
 * Meta Box JavaScript
 *
 * @package ApprenticeshipConnect
 */

/**
 * Initialize meta box functionality
 */
document.addEventListener( 'DOMContentLoaded', () => {
	// Meta box initialised – field interactions handled natively by ACF/SCF.
} );
